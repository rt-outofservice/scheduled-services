# Scheduled Services

Standalone Python scripts replacing Claude Code plugin skills. Business logic is algorithmic; AI (claude/codex) is invoked only when genuinely needed.

## Directory Layout

```
common/helpers/       - Shared Python modules (logging, telegram, ai)
services/<name>/      - Each service: main script, config.yaml, .bindeps
scripts/              - Utility scripts (migration, etc.)
docs/plans/           - Implementation plans
playbook.main.yaml    - Spot deployment playbook
inventory.yml         - Host inventory for spot
env.example-main.yml  - Environment variable template
env.<host>-main.yml   - Per-host configuration (not committed)
.tool-versions        - Python version pin (asdf/mise)
```

## Testing

Every Python script with business logic includes embedded unittest tests, runnable via:

```
uv run python <script>.py --tests
```

Tests use `unittest` and are gated behind `if __name__ == "__main__"` with `--tests` argument parsing.

## Common Helpers

Scripts import shared utilities via sys.path manipulation:

```python
import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parents[2] / "common" / "helpers"))
from log import setup_logging
from telegram import send_telegram
from ai import call_ai, call_ai_json, AIError
```

The `ai` module supports multiple providers via `call_ai(prompt, provider, model, effort)`:
- `provider="claude"` (default): invokes `claude -p [--model MODEL]`
- `provider="codex"`: invokes `codex exec [-m MODEL] [-c reasoning_effort=EFFORT]`

Both `call_ai` (text) and `call_ai_json` (JSON-parsed) accept the same provider/model/effort args and raise `AIError` on failures.

## Config

Each service has a `config.yaml` generated by the spot playbook from environment variables. Cron handles all scheduling - scripts execute unconditionally when triggered.

Common config keys across services:
- `hostname` — machine name for Telegram message headers
- `llm_provider` — "claude" (default) or "codex"
- `llm_model` — model name (e.g. "haiku", "gpt-5.4")
- `llm_model_effort` — reasoning effort for codex (e.g. "xhigh")

Telegram messages use the format: `\[hostname] *Service Title* (date)` as header.

## Error Handling

Never fail silently. All fatal errors are logged AND sent via telegram. Non-fatal errors are logged and appended to the normal telegram message.

## Logging

Daily log files at `~/.logs/scheduled-services/<service_name>/`. 30 most recent files kept per service.

## Service CLI Arguments

Beyond `--tests`, some services accept additional flags:

- `news_digest.py --digest-names tech,security` — process only named feed groups
- `pr_auto_approve.py --day-summary-only` — send day summary from reviews.md, then exit
- `fetch_feeds.py --hours 24` — standalone feed fetch with time window (reads JSON from stdin)
- `teams_summary.py --timeframe 14h` — override config timeframe (Nh/Nm/Nd format)
- `teams_summary.py --notify-on-match "keyword1,keyword2"` — keyword monitoring mode instead of summary

## Persistent State

Some services maintain state files in their service directory (preserved across deploys):

- `services/pr-auto-approve/reviews.md` — append-only log of all reviewed PRs/MRs
- `services/slack-summary/user-cache.txt` — cached Slack user ID to display name mappings (USERID=DisplayName per line)

## Commands

- Run tests: `uv run python <script>.py --tests`
- Lint: `uv run ruff check .`
- Format: `uv run ruff format --check .`
- Pre-commit: `pre-commit run --all-files`
